import React from "react";
import AccountPage from "../../features/account/ui/AccountPage/AccountPage";

const AccountPageContainer: React.FC = () => {
  return <AccountPage />;
};

export default AccountPageContainer;
